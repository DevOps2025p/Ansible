- name: loops playbook
  hosts: local
  connection: local
  tasks:
  - name: print the names
    ansible.builtin.debug:
      msg: "Hi, {{ item }}"
    loop:
      - "Ramesh"
      - "Mahesh"
      - "Suresh"
      - "Rajesh"