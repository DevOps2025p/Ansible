- name: packages state playbook
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
      - { name: "nginx", state: "absent" }
      - { name: "httpd", state: "absent" }
      - { name: "git", state: "present" }
      - { name: "telnet", state: "present" }
      